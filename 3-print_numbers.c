#include <stdio.h>
#include "main.h"

void print_numbers(void)
{

    int i, a, x = 9;
    for (i = 0; i <= a; i++)
    {
        while (x < 9)
        {
            x++;
        }
        x = 9;
        printf("\n");
    }
    return 0;
}